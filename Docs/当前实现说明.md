# 当前实现说明

当前实现以 `Docs/重构后使用说明.md` 和仓库根目录 `README.md` 为准。

核心能力：

- USB 串口和同一局域网 UDP 两种通信方式。
- WiFiManager 非阻塞手机 AP 配网。
- `AUTO`、`WIRED`、`WIRELESS` 三种可持久化通信模式。
- 电脑端每 2 秒发送状态心跳，ESP32 以 6 秒超时判定连接断开。
- 未连接时 GPIO5 黄灯闪烁。
- 建立连接时 GPIO6 绿灯闪烁 2 秒，随后显示真实状态。

旧的 `wifi_secrets.h`、UDP token 配对和阻塞式配网页流程已移除。
